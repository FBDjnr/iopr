% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gini_bstr.R
\name{gini_bstr}
\alias{gini_bstr}
\title{Bootstrap variance of the Gini index}
\usage{
gini_bstr(
  x,
  stratum = NULL,
  cluster = NULL,
  weight = NULL,
  data = NULL,
  nboot = 1000,
  parallel = FALSE,
  no_cores = NULL
)
}
\arguments{
\item{x}{A numeric vector of outcome values. If \code{data} is supplied, a bare
column name or a character column name may be given instead.}

\item{stratum}{Stratum ID vector. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID vector. Defaults to individual observations if
\code{NULL}.}

\item{weight}{Sampling weight vector. Defaults to equal weights if \code{NULL}.}

\item{data}{Optional data frame. If provided, \code{x}, \code{stratum}, \code{cluster}, and
\code{weight} are evaluated in the context of \code{data}.}

\item{nboot}{Number of bootstrap replicates. A positive integer; default is
\code{1000}.}

\item{parallel}{Logical; use parallel computation? Default is \code{FALSE}.}

\item{no_cores}{Number of cores to use when \code{parallel = TRUE}. Defaults to
all available cores minus 2.}
}
\value{
A named list with elements:
\itemize{
\item \code{est}: Gini index estimate on the original sample.
\item \code{var}: Bootstrap variance estimate.
\item \code{thetastar}: Numeric vector of bootstrap Gini estimates.
}
}
\description{
Estimates the variance of the Gini index via stratified cluster bootstrap
resampling. Observations with missing values are dropped before resampling.
}
