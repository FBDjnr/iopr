% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_data.R
\name{prep_data}
\alias{prep_data}
\title{Prepare survey data for inequality analysis}
\usage{
prep_data(
  x,
  stratum = NULL,
  cluster = NULL,
  hh_id = NULL,
  hh_size = NULL,
  circumstances = NULL,
  weight = NULL,
  data = NULL
)
}
\arguments{
\item{x}{Outcome variable (bare name or expression referencing a column in
\code{data}).}

\item{stratum}{Stratum ID variable. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID variable. Defaults to individual observations if
\code{NULL}.}

\item{hh_id}{Household ID variable. Defaults to row index if \code{NULL}.}

\item{hh_size}{Number of household members. Defaults to 1 if \code{NULL}.}

\item{circumstances}{Optional circumstance variable. Omitted from the output
if \code{NULL}.}

\item{weight}{Sampling weight. Defaults to \code{hh_size} if \code{NULL}.}

\item{data}{Optional data frame. If provided, all other arguments are
evaluated in the context of \code{data}.}
}
\value{
A data frame with columns \code{stratum}, \code{cluster}, \code{hh_id},
\code{hh_size}, \code{circumstances} (if supplied), \code{weight}, the outcome variable,
\code{H_s} (clusters per stratum), and \code{M_scs} (households per cluster-stratum).
}
\description{
Attaches cluster size, household count, and household size information to
the data, combines duplicate households by summing the outcome variable, and
returns a tidy data frame ready for use in \code{\link[=gini_index]{gini_index()}} or \code{\link[=iop_rel]{iop_rel()}}.
}
