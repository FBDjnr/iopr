% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iop_ablation_study.R
\name{iop_ablation_study}
\alias{iop_ablation_study}
\title{Ablation study for relative inequality of opportunity}
\usage{
iop_ablation_study(
  x,
  stratum = NULL,
  cluster = NULL,
  weight = NULL,
  circumstances,
  data = NULL,
  distribution = c("smoothed", "standardized")
)
}
\arguments{
\item{x}{A numeric vector of strictly positive outcome values.}

\item{stratum}{Stratum ID vector. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID vector. Defaults to individual observations if
\code{NULL}.}

\item{weight}{Sampling weight vector. Defaults to equal weights if \code{NULL}.}

\item{circumstances}{A data frame (or coercible object) of at least two
circumstance variables, or, when \code{data} is supplied, a character vector of
at least two column names.}

\item{data}{Optional data frame.}

\item{distribution}{\code{"smoothed"} (default) or \code{"standardized"}. The
\code{"smoothed"} method uses fitted values from the log-linear regression;
\code{"standardized"} additionally controls for residual variation at the
mean of circumstances.}
}
\value{
A data frame with one row per circumstance and columns:
\describe{
\item{circumstance}{Name of the ablated circumstance.}
\item{iop_full}{Relative IOP using all circumstances.}
\item{iop_reduced}{Relative IOP with this circumstance removed.}
\item{contribution}{\code{iop_full - iop_reduced}.}
\item{pc_contribution}{Percentage contribution,
\code{100 * contribution / iop_full}.}
}
}
\description{
Quantifies how much each circumstance variable contributes to relative
inequality of opportunity (IOP) by leave-one-out ablation. Relative IOP is
computed once using all circumstances (the full model) and then again with
each circumstance removed in turn. The drop in relative IOP when a
circumstance is removed measures that circumstance's contribution: a large
drop means the circumstance explains much of the opportunity-driven
inequality.
}
\details{
All models are fit on the same complete-case sample (rows with a missing
outcome, design variable, weight, or circumstance are dropped once, up front)
so the full and reduced estimates are comparable.
}
\examples{
round1 <- subset(synthetic_survey, round == "round_1")

# Contribution of each circumstance to relative IOP (leave-one-out)
iop_ablation_study(
  x = expenditure, stratum = stratum, cluster = cluster, weight = weight,
  circumstances = c("social_group", "religion", "land_category", "hh_type"),
  data = round1
)
}
\seealso{
\code{\link[=iop_rel]{iop_rel()}}, \code{\link[=iop_ablation_test]{iop_ablation_test()}}
}
