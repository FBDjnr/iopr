% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gini_index.R
\name{gini_index}
\alias{gini_index}
\title{Gini index and its variance}
\usage{
gini_index(
  x,
  stratum = NULL,
  cluster = NULL,
  weight = NULL,
  data = NULL,
  variance = TRUE,
  var.decompose = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric vector of outcome values. If \code{data} is supplied, a bare
column name or a character column name may be given instead.}

\item{stratum}{Stratum ID vector. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID vector. Defaults to individual observations if
\code{NULL}.}

\item{weight}{Sampling weight vector. Defaults to equal weights if \code{NULL}.}

\item{data}{Optional data frame. If provided, \code{x}, \code{stratum}, \code{cluster}, and
\code{weight} are evaluated in the context of \code{data}.}

\item{variance}{Logical; compute the variance? Default is \code{TRUE}.}

\item{var.decompose}{Logical; decompose the variance using the Bhattacharya
method? Default is \code{FALSE}. Only used when \code{variance = TRUE}.}

\item{...}{Currently unused.}
}
\value{
\itemize{
\item If \code{variance = FALSE}: a scalar Gini estimate.
\item If \code{variance = TRUE} and \code{var.decompose = FALSE}: a list with elements
\code{est} and \code{var}.
\item If \code{variance = TRUE} and \code{var.decompose = TRUE}: a list with elements
\code{est}, \code{var}, and \code{var.decompose}.
}
}
\description{
Computes the Gini index and, optionally, its variance using either the
influence function approach or the Bhattacharya decomposition, for complex
survey designs with stratification and clustering. Observations with missing
values are dropped before computation.
}
\examples{
round1 <- subset(synthetic_survey, round == "round_1")

# Design-based Gini index of expenditure with analytical variance
gini_index(
  x = expenditure, stratum = stratum, cluster = cluster,
  weight = weight, data = round1
)
}
\seealso{
\code{\link[=gini_ineq]{gini_ineq()}}, \code{\link[=gini_var]{gini_var()}}, \code{\link[=gini_var_bhatt]{gini_var_bhatt()}}
}
