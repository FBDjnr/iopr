% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iop_rel.R
\name{iop_rel}
\alias{iop_rel}
\title{Relative Inequality of Opportunity}
\usage{
iop_rel(
  x,
  stratum = NULL,
  cluster = NULL,
  weight = NULL,
  circumstances,
  data = NULL,
  variance = FALSE,
  var.decompose = FALSE,
  distribution = c("smoothed", "standardized")
)
}
\arguments{
\item{x}{A numeric vector of strictly positive outcome values.}

\item{stratum}{Stratum ID vector. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID vector. Defaults to individual observations if
\code{NULL}.}

\item{weight}{Sampling weight vector. Defaults to equal weights if \code{NULL}.}

\item{circumstances}{A vector, factor, or data frame of circumstance
variables. If \code{data} is provided, may instead be a character vector of
column names.}

\item{data}{Optional data frame.}

\item{variance}{Logical; compute the variance? Default is \code{FALSE}.}

\item{var.decompose}{Logical; use Bhattacharya decomposition for variance?
Default is \code{FALSE}. Only relevant when \code{variance = TRUE}.}

\item{distribution}{\code{"smoothed"} (default) or \code{"standardized"}. The
\code{"smoothed"} method uses fitted values from the log-linear regression;
\code{"standardized"} additionally controls for residual variation at the
mean of circumstances.}
}
\value{
\itemize{
\item If \code{variance = FALSE}: a named list with \code{total_iop}, \code{abs_iop}, and
\code{rel_iop}.
\item If \code{variance = TRUE}: a data frame with rows \code{total_iop}, \code{abs_iop},
\code{rel_iop} and columns \code{est} and \code{var} (plus decomposed columns if
\code{var.decompose = TRUE}).
}
}
\description{
Estimates relative IOP by regressing log-outcome on circumstance variables
and computing the Gini index of the resulting hypothesized distribution.
Optionally computes the variance using analytical or decomposed methods.
}
\examples{
round1 <- subset(synthetic_survey, round == "round_1")

# Relative IOP from a set of circumstance variables (point estimate)
iop_rel(
  x = expenditure,
  circumstances = c("social_group", "religion", "land_category"),
  data = round1
)

# With the survey design and analytical variance
iop_rel(
  x = expenditure, stratum = stratum, cluster = cluster, weight = weight,
  circumstances = c("social_group", "religion", "land_category"),
  data = round1, variance = TRUE
)
}
