% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iop_var.R
\name{iop_var}
\alias{iop_var}
\title{Variance of the relative Inequality of Opportunity}
\usage{
iop_var(
  x,
  y,
  stratum = NULL,
  cluster = NULL,
  weight = NULL,
  distribution = c("smoothed", "standardized")
)
}
\arguments{
\item{x}{A numeric vector of observed outcome values.}

\item{y}{A numeric vector of the hypothesized "fair" outcome values (either
standardized or smoothed, matching the \code{distribution} argument of
\code{\link[=iop_rel]{iop_rel()}}). Must be the same length as \code{x}.}

\item{stratum}{Stratum ID vector. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID vector. Defaults to individual observations if
\code{NULL}.}

\item{weight}{Sampling weight vector. Defaults to equal weights if \code{NULL}.}

\item{distribution}{Character; \code{"smoothed"} or \code{"standardized"}. Controls
whether the absolute IOP is \code{G(y)} (smoothed) or \code{G(x) - G(y)}
(standardized).}
}
\value{
A data frame with rows \code{total_iop}, \code{abs_iop}, \code{rel_iop} and
columns \code{est} (point estimate) and \code{var} (variance estimate).
}
\description{
Estimates the variance of the relative IOP (and its components) using the
influence function approach for stratified cluster samples.
}
