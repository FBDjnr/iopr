% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iop_rel_var_bstr.R
\name{iop_rel_var_bstr}
\alias{iop_rel_var_bstr}
\title{Bootstrap variance of relative Inequality of Opportunity}
\usage{
iop_rel_var_bstr(
  x,
  stratum = NULL,
  cluster = NULL,
  weight = NULL,
  circumstances,
  data = NULL,
  distribution = c("smoothed", "standardized"),
  nboot = 1000,
  parallel = FALSE,
  no_cores = NULL
)
}
\arguments{
\item{x}{A numeric vector of strictly positive outcome values.}

\item{stratum}{Stratum ID vector. Defaults to a single stratum if \code{NULL}.}

\item{cluster}{Cluster ID vector. Defaults to individual observations if
\code{NULL}.}

\item{weight}{Sampling weight vector. Defaults to equal weights if \code{NULL}.}

\item{circumstances}{A vector, factor, or data frame of circumstance
variables. If \code{data} is provided, may instead be a character vector of
column names.}

\item{data}{Optional data frame.}

\item{distribution}{\code{"smoothed"} (default) or \code{"standardized"}.}

\item{nboot}{Number of bootstrap replicates. A positive integer; default is
\code{1000}.}

\item{parallel}{Logical; use parallel computation? Default is \code{FALSE}.}

\item{no_cores}{Number of cores to use when \code{parallel = TRUE}.}
}
\value{
A named list with elements:
\itemize{
\item \code{est}: Relative IOP estimate on the original sample.
\item \code{var}: Bootstrap variance estimate.
\item \code{theta.cov}: Bootstrap covariance between numerator and denominator.
\item \code{thetastar}: Matrix of bootstrap IOP estimates.
}
}
\description{
Estimates the bootstrap variance of the relative IOP via stratified cluster
resampling.
}
